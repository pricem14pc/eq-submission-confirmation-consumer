platform: linux
image_resource:
  type: docker-image
  source:
    repository: alpine
inputs:
  - name: eq-submission-confirmation-consumer
outputs:
  - name: extracted-submission-confirmation-consumer-image
run:
  path: sh
  args:
    - -exc
    - |
      tar -czvf source.tar.gz /path/to/dir1 eq-submission-confirmation-consumer/deploy-image
      cat Dockerfile > extracted-submission-confirmation-consumer-image
